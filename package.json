{
  "name": "verifi",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && cp dist/v.js v.js && node scripts/build-html.mjs",
    "build:html": "node scripts/build-html.mjs",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.31.0",
    "vite": "^5.4.0"
  }
}
